<h3>Import fits</h3>
<div class="container-input">
    <p>Use this panel to import your fits into the shopping list. You can see more information about how to export them in the help.</p>
@*    @using (Ajax.BeginForm("AnalyzeRawFit", "Lists", new AjaxOptions { UpdateTargetId = "fitsAnalysed", OnSuccess = "ReactivateImportedFitsAccordion", OnFailure = "onFailureAnalyzeRawFit" }))
    {
        *@<form id="payment">
            <fieldset>
                <legend></legend>
            <span class="btn btn-success fileinput-button">
                <i class="glyphicon glyphicon-plus"></i>
                <a class="buttonYellow-short">Upload XML file</a>
                <input id="fu-my-auto-upload" type="file" name="files" accept="text/xml">
                <span>Or paste in this text area any fit in supported formats (EFT, XML or DNA):</span>
                <!-- The file input field used as target for the file upload widget -->
            </span>
                <textarea id="rawFit" name="rawFit" rows="5" placeholder="Paste here the fit that you want to import from EVE or supported fitting tools. Or upload an EVE XML file." required></textarea>
                <a id="idAnalyseBtn" class="buttonYellow-short">Analyse</a>
@*                <button id="idAnalyseBtn" type="submit"  value="Analyse">Analyse</button>*@
            </fieldset>
        </form>

@*    }*@
    <div id="fitsAnalysed" class="subs-wrapper" data-esh-analysed-fits>
    </div>
    <p style="font-size:small;">Tip: Fits posted in EVE chats are in DNA format, you could paste the chat message here and will show in DNA.</p>
</div>
